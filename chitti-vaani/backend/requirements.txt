flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
httpx==0.27.2
# Admin Dashboard (product Gmail accounts + monthly keep-alive scheduler)
apscheduler==3.10.4
sqlalchemy==2.0.36
sqlalchemy-libsql==0.2.0             # Turso dialect (we use embedded replica, not direct Hrana) — see [[project_turso_embedded_replica_pattern]]
libsql-experimental==0.0.55          # cp311 manylinux wheel; owns the Turso sync protocol — used by database.py:resolve_db_url
psycopg2-binary==2.9.9
tzdata==2024.2

# Sahay AI quality framework deps (used by lib/*.py)
prometheus-client==0.20.0    # /metrics endpoint; lazy-imported in observability

# Chitti Document Vault (Phase 1) — per-user Fernet encryption at rest.
# AES-128-CBC + HMAC-SHA256 via the cryptography lib; deterministic
# per-user key derivation in services/vault_service.py.
cryptography==43.0.1
