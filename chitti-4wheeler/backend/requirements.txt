# Chitti 4-Wheeler — backend dependencies
# Pinned for Render free tier (Python 3.11). Pure Python or manylinux
# wheels — NO Rust toolchain required. Mirrors chitti-news pattern.

flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

# Turso embedded-replica pattern (SAHAYAI_MASTER §2 row 2).
sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0
libsql-experimental==0.0.55

# DeepSeek HTTP client (sole LLM provider per §2 row 1).
httpx==0.27.2

python-dotenv==1.0.0
