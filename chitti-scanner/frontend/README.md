# Chitti Product Scanner — frontend

Single static page mirrored from the canonical `chitti_scanner.html` at the repo root.

URL params:
- `?api=https://my-host` — point at a non-default Scanner backend
- `?medupi=https://medupi-host` — point Jan-Aushadhi inline lookups at a non-default MedUPI backend
