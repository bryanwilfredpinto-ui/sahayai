# Chitti Jobs — Flask backend dependencies.
# Pinned for Railway/Render free tier (Python 3.11). Pure Python or
# manylinux wheels — no Rust toolchain required.

flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0
libsql-experimental==0.0.55

requests==2.31.0
httpx==0.27.2
python-dotenv==1.0.0

# RSS job-source parsing (Naukri RSS + Indeed RSS) — pure Python.
feedparser==6.0.11

# In-process scheduler — daily 07:00 IST job-source poll.
apscheduler==3.10.4
tzdata==2024.2

# Fuzzy matching for role-title scoring + ATS keyword overlap.
rapidfuzz==3.6.1

# Sahay AI quality framework deps (lib/*.py)
prometheus-client==0.20.0
