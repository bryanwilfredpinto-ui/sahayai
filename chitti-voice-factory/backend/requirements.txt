flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
httpx==0.27.2
requests==2.31.0                     # used by suppliers/bhashini.py and friends
sqlalchemy==2.0.23
sqlalchemy-libsql==0.2.0             # Turso dialect (we use embedded replica, not direct Hrana)
libsql-experimental==0.0.55          # cp311 manylinux wheel; owns the Turso sync protocol

# ── Fluency pipeline (textbook + Wikipedia ingestion → multilingual embeddings) ──
numpy>=1.26,<2.0                     # required by sentence-transformers + faiss
pymupdf==1.24.10                     # PDF text extraction (best Indic-script support)
sentence-transformers==3.0.1         # paraphrase-multilingual-MiniLM-L12-v2 (50+ langs)
faiss-cpu==1.8.0                     # IndexFlatIP for per-language fluency search
youtube-transcript-api>=1.2.4,<2.0   # user-submitted YouTube videos → fluency corpus (1.x API; 0.6.x is broken against current YouTube)

# Sahay AI quality framework deps (used by lib/*.py)
prometheus-client==0.20.0    # /metrics endpoint; lazy-imported in observability
apscheduler==3.10.4          # hosts the daily founder-report cron
tzdata==2024.2               # IST timezone for apscheduler
