# Chitti Shares - Backend Dependencies (Phase 1)
# Pinned versions for reproducible deploys

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0             # Turso dialect (we use embedded replica, not direct Hrana)
libsql-experimental==0.0.55          # cp311 manylinux wheel; owns the Turso sync protocol
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1
email-validator==2.2.0

# ===== Phase 2: Market Data =====
kiteconnect==5.0.1
tzdata==2024.2
pandas>=2.0.0
numpy>=1.24.0

# ===== Phase 3-5: Yahoo Finance + features =====
yfinance==0.2.50

# ===== Phase 6: In-process scheduler (replaces external cron jobs) =====
apscheduler==3.10.4
pyotp==2.9.0
