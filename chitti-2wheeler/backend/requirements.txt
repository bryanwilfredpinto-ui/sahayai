# Chitti 2-Wheeler — backend dependencies
# Pinned for Render free tier (Python 3.11). Pure Python or manylinux
# wheels — NO Rust toolchain required. Mirrors chitti-news pattern.

flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

# Turso embedded-replica pattern (SAHAYAI_MASTER §2 row 2). Pinned —
# direct Hrana is broken with sqlalchemy-libsql, see
# [[project_turso_embedded_replica_pattern]].
sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0
libsql-experimental==0.0.55

# DeepSeek HTTP client — OpenAI-compatible REST against api.deepseek.com
# (SAHAYAI_MASTER §2 row 1 — DeepSeek is sole LLM provider).
httpx==0.27.2

python-dotenv==1.0.0
