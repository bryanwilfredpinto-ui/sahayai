# Chitti UPI Fraud Guard — frontend

Single static page mirrored from the canonical `chitti_upi.html` at the repo root. Edit there, copy here when shipping.

To point at a non-default backend, append `?api=https://my-host` to the URL.
