# Chitti News - Flask backend dependencies
# Pinned for Render free tier (Python 3.11). Pure Python or manylinux
# wheels — NO Rust toolchain required. Mirrors chitti-medupi pattern.

flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0             # Turso libSQL dialect (cp311 linux wheels exist for both this + transitive dep)
libsql-experimental==0.0.55          # pin transitive dep — Rust client w/ TLS by default, pre-built manylinux wheel

requests==2.31.0
python-dotenv==1.0.0

# Cloudflare TLS-fingerprint bypass — required for Saamana, Prajavani,
# Varthabharati, Rozana Spokesman and other regional publishers whose
# RSS endpoints sit behind Cloudflare bot-detection. news_ingest.py
# imports it lazily and falls back to requests-only if absent (so a
# pip failure doesn't break the whole poll loop). Added 2026-06-02.
cloudscraper==1.2.71

# RSS / Atom parsing — pure Python, handles every common dialect
feedparser==6.0.11

# Article-body extraction for speakers — when an RSS publisher ships
# only a 300-char summary (most regional sources), services/article_body.py
# fetches the article URL on demand, extracts the main content, and
# caches it to Article.content so blind users hear the FULL news, not
# just the headline blurb. Sire 2026-06-02. Pure Python; no lxml.
beautifulsoup4==4.12.3

# DeepSeek HTTP client — OpenAI-compatible REST, used by news_summary.py
# and news_explain.py against api.deepseek.com. Anthropic SDK removed
# per project_ai_provider_switch_to_deepseek (DeepSeek-only LLM provider).
httpx==0.27.2

# Fuzzy title matching for cross-source fact-checking + dedupe
rapidfuzz==3.6.1

# In-process scheduler — drives hourly RSS poll + breaking-news watcher.
apscheduler==3.10.4
tzdata==2024.2

# Sahay AI quality framework deps (used by lib/*.py)
prometheus-client==0.20.0    # /metrics endpoint; lazy-imported in observability
