# Chitti MedUPI - Backend dependencies
# Pinned for reproducible deploys (Render free tier, Python 3.11).
#
# Pydantic v1 (no Rust dependency — pure-Python wheels work on every
# platform). Render's slim image lacks Rust + cmake, so any v2 install
# fails at pydantic-core's compile step. v1's BaseSettings lives inside
# pydantic itself, so we drop the separate pydantic-settings package.
# FastAPI is matched to the last v1-native release (0.95.2).

fastapi==0.95.2                  # last release with native pydantic v1
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35

pydantic==1.10.13                # pure Python; BaseSettings is in pydantic itself

python-multipart==0.0.6          # matches FastAPI 0.95.2's expectations
httpx==0.27.2
python-dotenv==1.0.1
psycopg2-binary==2.9.9            # binary distro — never compiles libpq
email-validator==2.2.0

# Anthropic SDK — used by medupi_recognition.py for image-based composition extraction.
# Vision-capable models read the medicine strip directly. No Tesseract install pain.
anthropic==0.39.0

# Fuzzy brand-name matching for typo-tolerant search ("crocon", "Crocine 650").
# 3.6.1 ships pre-built manylinux wheels for Python 3.11; the fuzz.WRatio +
# process.extract API is unchanged from the latest release.
rapidfuzz==3.6.1

# In-process scheduler — drives monthly Jan Aushadhi / weekly NPPA /
# daily 02:00 IST top-100 Brave refresh / cache eviction.
apscheduler==3.10.4
tzdata==2024.2

# ===== Real-data loader (scripts/load_real_data.py) =====
# CSV / Excel parsing for the NPPA + Jan Aushadhi + CDSCO downloads.
# Optional at runtime — only the loader scripts import these.
pandas>=2.0.0
openpyxl>=3.1.0
