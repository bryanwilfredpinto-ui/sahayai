# Chitti MedUPI - Flask backend dependencies
# Pinned for Render free tier (Python 3.11). Pure Python or manylinux
# wheels — NO Rust toolchain required anywhere.

flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

sqlalchemy==2.0.35
sqlalchemy-libsql==0.2.0             # Turso dialect (we use embedded replica, not direct Hrana)
libsql-experimental==0.0.55          # cp311 manylinux wheel; owns the Turso sync protocol
psycopg2-binary==2.9.10             # kept temporarily for data-export tooling; will remove after cutover verified

requests==2.31.0
httpx>=0.27,<1.0                # DeepSeek vision call (OpenAI-compatible chat completions)
python-dotenv==1.0.0

# DeepSeek vision via OpenAI-compatible /chat/completions endpoint —
# replaces Anthropic per the LOCKED §2 decision (DeepSeek is the sole LLM
# provider across every Chitti backend). No SDK needed; we use httpx
# directly with the same image_url data-URL pattern as chitti-scanner.
# project_ai_provider_switch_to_deepseek

# Fuzzy brand-name matching for the /medicine search.
# 3.6.1 ships pre-built manylinux wheels for Python 3.11.
rapidfuzz==3.6.1

# In-process scheduler — drives monthly Jan Aushadhi / weekly NPPA /
# daily 02:00 IST top-100 Brave refresh / 24h cache evict.
apscheduler==3.10.4
tzdata==2024.2

# ===== Real-data loader (scripts/load_real_data.py) =====
# CSV / Excel parsing for the NPPA + Jan Aushadhi + CDSCO downloads.
# Optional at runtime — only the loader scripts import these.
pandas>=2.0.0
openpyxl>=3.1.0

# Sahay AI quality framework deps (used by lib/*.py)
prometheus-client==0.20.0    # /metrics endpoint; lazy-imported in observability

# ===== Chitti Health File (added 2026-05-23) =====
# AES-256-GCM at rest for encrypted health documents — non-negotiable
# privacy contract per CHITTI_HEALTH_FILE_MASTER_SPEC.md.
# Pure-Python wheels available for cp311 manylinux; no Rust toolchain.
cryptography>=42.0,<46.0

# Phase B-5 (2026-05-24) — Doctor PDF export. Pure-Python, ships
# pre-built wheels for every platform incl. Render free tier. Uses the
# built-in Helvetica fonts so no system-font install is required.
reportlab>=4.0,<5.0
